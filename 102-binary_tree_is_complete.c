#include "binary_trees.h"

/**
* binary_tree_is_complete - binary_tree_is_complete
* @tree: node
* Return: is complete indicator
*/
int binary_tree_is_complete(const binary_tree_t *tree)
{
	int is_complete = 0;

	return (is_complete);
}
